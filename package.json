{
  "name": "es2015-app",
  "version": "0.0.1",
  "scripts": {
    "watch": "watchify -d -t babelify src/app.es6 -o dist/build.js",
    "serve": "browser-sync start --server --files dist/build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t babelify src/app.es6 -o dist/build.js"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browser-sync": "^2.11.1",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  }
}
